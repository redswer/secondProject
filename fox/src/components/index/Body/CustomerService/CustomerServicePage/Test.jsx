function Test ( { notice_content_selected } ) {
    const {id, content} = notice_content_selected;
    return (
        <div>
            아...개빡친다
            <div>{id}</div>
            <div>{content}</div>
        </div>
    )
}

export default Test;